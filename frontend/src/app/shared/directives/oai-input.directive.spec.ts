import { OaiInputDirective } from './oai-input.directive';

describe('OaiInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OaiInputDirective();
    expect(directive).toBeTruthy();
  });
});
